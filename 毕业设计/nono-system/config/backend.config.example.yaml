server:
  host: "0.0.0.0"
  port: 8080
  mode: "debug"  # debug 或 release

blockchain:
  rpc_url: "http://localhost:8545"  # 区块链节点RPC地址
  chain_id: 1  # 链ID
  contract_addr: "0x0000000000000000000000000000000000000000"  # 部署合约后替换
  private_key: ""  # 用于签名交易的私钥（不含0x前缀），留空则禁用区块链功能

database:
  host: "localhost"
  port: 5432
  user: "nono"
  password: "nono123"
  db_name: "nono_system"

redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0

